import {
  PayContainer,
  PayButton,
} from "../components/styles/Payment/payment.styled";
const SuccessPay = () => {
  return (
    <PayContainer>
      <PayButton>Success Pay</PayButton>
    </PayContainer>
  );
};

export default SuccessPay;
